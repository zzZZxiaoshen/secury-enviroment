<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.marsfood.dao.mall.OrderMapper">

    <select id="selectOrderCount" resultType="java.lang.Integer">
        SELECT
          COUNT(`ID`)
        FROM
          `t_order`
    </select>

    <select id="selectOrderList" resultType="OrderCountDo">
        SELECT
          DATE_FORMAT(`create_date`,'%Y-%m-%d') orderDate,
          COUNT( `create_date` ) countOrder
        FROM
          `t_order`
        WHERE
          DATEDIFF( `create_date`, NOW( ) ) &lt; 0
        AND DATEDIFF( `create_date`, NOW( ) ) >-8
        GROUP BY
          DATE_FORMAT(`create_date`,'%Y-%m-%d')
    </select>

</mapper>